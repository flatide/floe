[package]
name = "floe-render-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Headless fixture and benchmark CLI for the floe CPU renderer"

[dependencies]
floe-render-core = { path = "../render-core" }
